import { h } from 'preact';
// import ant-mobile for test common chunk
import { Button } from 'antd-mobile';

const About = () => (
    <div>
        <h2>About</h2>
        <Button type="primary">Primary</Button>
    </div>
);

export default About;